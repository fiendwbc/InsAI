# Docker build context exclusions
# Prevents sensitive files from being copied during `docker build`

# Secrets and credentials (CRITICAL - never include in image)
.env
.env.*
!.env.example

# Database files (mount as volumes instead)
*.db
*.db-journal
*.db-wal
*.db-shm

# Log files (mount as volumes instead)
logs/
*.log

# Git repository (not needed in image)
.git/
.gitignore
.gitattributes

# Python cache and bytecode
__pycache__/
*.py[cod]
*$py.class
*.so
.Python

# Virtual environments (uv creates these locally)
.venv/
venv/
ENV/
env/

# Development tools
.vscode/
.idea/
*.swp
*.swo
*~

# Testing artifacts
.pytest_cache/
.coverage
htmlcov/
.tox/
.hypothesis/

# Build artifacts
build/
dist/
*.egg-info/
.eggs/

# Documentation (not needed in runtime image)
docs/
*.md
!README.md

# Specification files (not needed in runtime)
specs/
TODO.md

# OS-specific files
.DS_Store
Thumbs.db
desktop.ini

# Temporary files
*.tmp
tmp/
temp/
